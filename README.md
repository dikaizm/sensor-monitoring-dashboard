# Sensor Monitoring System

## Documentation

```bash
project-root/
│
├── src/
│   ├── controllers/
│   │   └── // Controller files (e.g., UserController.ts, AuthController.ts)
│   ├── routes/
│   │   └── // Route files (e.g., userRoutes.ts, authRoutes.ts)
│   ├── models/
│   │   └── // Model files (e.g., UserModel.ts)
│   ├── middleware/
│   │   └── // Middleware files (e.g., authentication.ts, errorHandling.ts)
│   ├── services/
│   │   └── // Service files (e.g., userService.ts)
│   ├── config/
│   │   └── // Configuration files (e.g., database.ts, environment.ts)
│   ├── utils/
│   │   └── // Utility files (e.g., validation.ts, logger.ts)
│   └── index.ts // Entry point of your application
│
├── node_modules/
├── package.json
├── tsconfig.json
└── .env // Environment variables file

```
